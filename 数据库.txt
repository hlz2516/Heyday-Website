sql语句执行过程
from，得到数据源
where对数据进行筛选过滤
group by对筛选后的数据分组
select 选取你要的字段
order by根据某个或多个字段进行顺序排列

什么是存储过程？
存储在数据库中的可执行对象。
自定义存储过程是用户使用T-SQL语句编写的，为了实现某一特定业务需求的，在用户数据库中编写的T-SQL语句的集合。

什么是事务？
事务就是单个逻辑工作单元执行的一系列操作，而这个操作必须要满足ACID属性，即原子性，一致性，隔离性，持久性

sqlserver的默认事务隔离级别：读已提交（read committed）
